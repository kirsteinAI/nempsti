
# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/
/GDrive

# macOS
.DS_Store

# Editors
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*

# Secrets / local env
.env
.env.*

# Patient data exports (DSGVO — never commit)
patientenstunden-*.json

# Internal docs (architecture, reviews) — not part of deployed app
/docs

# Local source material (Excel planning aids, reference files)
/src

# Testing & development — not needed on GitHub Pages (production).
# Tests run locally only (no CI configured). These files stay on disk
# but must not be served to production via GitHub Pages.
/tests
playwright.config.ts
test-mapping.json
run-tests.sh

# Dev tooling config — not part of the deployed app
package.json
package-lock.json
.claude/
CLAUDE.md