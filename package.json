{
  "name": "nempsti",
  "version": "1.0.0",
  "description": "Patientenstunden-Tracker (PWA)",
  "type": "module",
  "scripts": {
    "test:unit": "node --test tests/unit",
    "test:e2e": "playwright test",
    "test": "npm run test:unit && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0"
  }
}
